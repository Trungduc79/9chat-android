<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._9chat"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme._9chat"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep links: custom scheme + web fallback.
                 All URIs flow through DeepLinkRouter.parseUri(). See
                 navigation/DeepLinkRouter.kt for the full pattern list. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="9chat" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="9chat.vn" />
                <data android:scheme="https" android:host="www.9chat.vn" />
            </intent-filter>
            <!-- Share target: appear in "Share via" picker for other apps -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>
        <!-- Bubble Activity (Android 11+ Bubbles API expanded container).
             Hệ thống embed Activity này vào bubble window khi user tap bubble peek.
             - allowEmbedded=true : required để system embed vào bubble container
             - resizeableActivity=true : bubble window mode khác full-screen
             - documentLaunchMode=always : mỗi room = 1 task riêng (multi-bubble)
             - taskAffinity="" : không gộp vào MainActivity task stack
             - exported=true : system process launch được -->
        <activity
            android:name=".ui.bubble.BubbleActivity"
            android:exported="true"
            android:launchMode="standard"
            android:taskAffinity=""
            android:documentLaunchMode="always"
            android:resizeableActivity="true"
            android:allowEmbedded="true"
            android:theme="@style/Theme._9chat"
            android:windowSoftInputMode="adjustResize" />
        <!-- FCM Service -->
        <service
            android:name=".service.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- Call action receiver (headless reject) -->
        <receiver
            android:name=".service.CallActionReceiver"
            android:exported="false" />
        <!-- Message action receiver (inline reply + mute 1h) -->
        <receiver
            android:name=".service.MessageActionReceiver"
            android:exported="false" />
        <!-- Foreground service that keeps the call alive when the app is
             backgrounded. Required on Android 12+ to prevent the system from
             killing the process and silently dropping the call. -->
        <service
            android:name=".call.service.CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone|camera|phoneCall" />
        <!-- FileProvider for granting read access to images that the gallery
             viewer downloads into cache before handing them to the system
             share chooser. Lets other apps open the image content directly
             rather than fetching the URL themselves. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
